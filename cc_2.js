// Tip Calculator
let bill = 275; // Example bill amount
let tip = (bill > 50 & bill = 300) bill * .1 : bill * 0.2;
console.log(`Initial logic for calculating tip using ternary operator: Bill = ${bill}, Tip = ${tip}`);

const bill = 275;
const tip = calculateTip(bill);
const total = bill + tip;
console.log(`The bill was ${bill}, the tip was ${tip.toFixed(2)}, total cost (bill+tip) was ${totalAmount.toFixed(2)}`);